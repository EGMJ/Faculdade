#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

typedef struct NodoArvore
{
    int chave, esquerda, direita;

};


void main(void){
    setlocale(LC_ALL, "Portuguese");

    // Instrodução a arvores
    /*
        - Arvore é um tipo de grapho
        - as conexoes em nodos sao representados por nicel hierarquico
        - 
        - pesquisar mais sobre arvores ordenadas para a busca binaria/ uma arvore binaria sempre tem dois filhos cada nodo
        - pesquisar as formas de representar uma arvore
        - o menor elemento de uma arvore é somente um nó ou o raiz
        - grau de um nó é a quantidade de filhos que ele tem
        - o maior grau da arvore é o maximo de nós que os filhos terao
        - Nó interno são os que possuem filhos
        - nós folha, sao os nós que tem um grau igual a zero
        - caminho em uma arvore é a sequencia de nos consecurivos
        - o nivel começa na raiz e vai paras as folhas já a altura é o contrario que começa pelos que nao tem filhos
    */



}